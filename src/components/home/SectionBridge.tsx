import { motion } from "framer-motion";

interface Props {
  text: string;
}

const SectionBridge = ({ text }: Props) => {
  return (
    <div className="py-8 text-center text-muted-foreground text-sm">{text}</div>
  );










};

export default SectionBridge;