import type { Metadata } from "next";
import RetailIndustry from "@/pages/RetailIndustry";

export const metadata: Metadata = {
  title: "Retail CX & AI Transformation | Fortay Connect",
  description:
    "Turn customer experience into competitive advantage. AI chatbots, omnichannel CX, seasonal scaling. Built for retail operations leaders who need results, not pilots.",
};

export default function RetailPage() { return <RetailIndustry />; }
